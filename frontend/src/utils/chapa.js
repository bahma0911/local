// src/utils/chapa.js
// Chapa utility removed — keep a small stub to avoid import errors if referenced.
export const CHAPA_CONFIG = { removed: true };
export const generateTxRef = () => `removed-${Date.now()}`;